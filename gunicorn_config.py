# Привязываем к порту, который Render ожидает
bind = "0.0.0.0:10000"

# Оптимальное количество воркеров для Render
workers = 2
threads = 2

# Увеличиваем таймаут для обработки длительных запросов
timeout = 120

# Включаем доступ к логам
accesslog = '-'
errorlog = '-'

# Настройки для лучшей производительности
worker_class = 'sync'
keepalive = 65
